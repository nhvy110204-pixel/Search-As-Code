global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - "alerting_rules.yml"

scrape_configs:
  - job_name: 'ragflash-backend'
    scrape_interval: 15s
    static_configs:
      - targets: ['host.docker.internal:8000']
